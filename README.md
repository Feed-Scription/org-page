# org-page

青椒模拟器团队（Feed-Scription）作品集站点。

纯静态单页 —— `index.html` + `assets/`，部署到 GitHub Pages 即可。

## 本地预览

```bash
python3 -m http.server 8000
# open http://localhost:8000
```

## 部署到 GitHub Pages

Repo Settings → Pages → Source = `main` / root，保存即生效。
